{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "mock-api": "json-server mock-api.json --routes mock-routes.json --port 5000",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "json-server": "^0.17.3",
    "nuxt": "3.0.0-rc.13"
  }
}
